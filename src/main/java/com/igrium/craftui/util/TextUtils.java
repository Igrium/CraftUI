package com.igrium.craftui.util;

import net.minecraft.text.Text;

/**
 * CraftUI has features to render text to the UI using special strings. This
 * class features a set of functions to generate said strings from Text objects.
 */
public final class TextUtils {

    /**
     * Generate a string that will render a given Text in a CraftApp.
     * 
     * @param text Text to render.
     * @return The string.
     */
    public static String getString(Text text) {
        // TODO: Actually implement text rendering lol
        return text.getString();
    }
}
